import '../session_1/1.dart';
import '../session_1/2.dart';

void main() {
  // 1
  ListOfNumbers list = new ListOfNumbers(numbersList: [12, 15, 13, 3]);
  // print(list.product());
  // 2
  Rectangle rectangle = new Rectangle(height: 6, width: 4);
  print(rectangle.perimeter());
}
